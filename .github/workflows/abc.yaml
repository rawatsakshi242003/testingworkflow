name: Hello World Action

on: 
   push:
      branches:
         - main
   workflow_dispatch:



jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Greet the user
        run: echo "Hello World! My first GitHub Action is running."